public class StringPractice2 {
    public static void main(String[] args) {
        String str = "abc";
        String rtr ;
        int c = 0;
        for (int i = 0; i < str.length(); i++) {
            for (int j = i; j < str.length() + 1; j++) {
                System.out.print(str.substring(i, j) + " ");
                rtr=str.substring(i, j);
                if (rtr.equals(rtr.)) {
                    
                } else {
                    
                }
            }
        }
    }
}
